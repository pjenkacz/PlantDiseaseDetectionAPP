<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet do API -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Kamera -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Zapis zdjęć (tylko do Android 9, maxSdkVersion = 28) -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />

    <!-- Info o kamerze -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SlamLeaf">

        <!-- CameraActivity NIE jest launcherem, NIE powinna być widoczna dla innych aplikacji -->
        <activity
            android:name=".ui.main.CameraActivity"
            android:exported="false" />

        <!-- MainActivity – też NIE powinna być widoczna z zewnątrz -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <!-- RegisterActivity – też tylko wewnątrz apki -->
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".PhotoDetailActivity"
            android:exported="false" />

        <!-- LoginActivity – LAUNCHER = musi mieć exported="true" -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>
